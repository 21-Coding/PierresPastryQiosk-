@{
  Layout = "_Layout";
}
@using Pierre.Models;
@model List<Pierre.Models.Treat>;
<div class="row">
    <div class="section-header">
     
    </div>
  <div class="col-md-6 offset-md-3">
    <h1 class="text-center">Treats</h1>

    @if (@Model.Count == 0)
    {
      <h3 class="text-center">No treats have been added yet!</h3>
    }
    else
    {
      <div class="list-group" id="list-tab" role="tablist">
        @foreach (Treat treat in Model)
        {
          <div class="list-group-item list-group-item-action">
            - @Html.ActionLink($"{treat.TreatName}", "Details", new { id = treat.TreatId }) 
              
          </div>
        }
      </div>
    }
    <br>
    <br>
  @if (User.Identity.IsAuthenticated)
  {
    <div class="text-center">
      @Html.ActionLink("➕", "Create", null, null, new {@class="btn btn-success"})

      @Html.ActionLink("🏠", "Index", "Home", null, new {@class="btn btn-info"})
    </div>
  }
  </div>
</div>